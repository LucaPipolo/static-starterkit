/*! #copyright DO NOT REMOVE# */

(function (project) {
  'use strict'
  project(window.jQuery, window, document)
})(function ($, window) {
  'use strict'
  // Declare here variables associated to your function names.
  $(function () {
    $(window).on('load', function () {
      // Call here functions that should be called on window load.
    })
    // Call here all the other functions.
  })
  // Declare here your functions.
})
